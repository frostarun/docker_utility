{
  "name": "docker_utility",
  "productName": "Docker SageMode Utility",
  "author": "Arun Kumar GR | frostarun@gmail.com",
  "version": "0.1.0",
  "main": "docker_utility.js",
  "devDependencies": {
    "angular-loading-bar": "^0.9.0",
    "electron": "^2.0.2",
    "electron-packager": "^12.1.0"
  },
  "scripts": {
    "package-win": "electron-packager . docker_utility --overwrite --asar=true --platform=win32 --arch=ia32 --icon=assets/icons/win/icon.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"Docker SageMode Utility\""
  }
}
